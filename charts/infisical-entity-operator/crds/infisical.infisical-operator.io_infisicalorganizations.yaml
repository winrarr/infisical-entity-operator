---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: infisicalorganizations.infisical.infisical-operator.io
spec:
  group: infisical.infisical-operator.io
  names:
    kind: InfisicalOrganization
    listKind: InfisicalOrganizationList
    plural: infisicalorganizations
    singular: infisicalorganization
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InfisicalOrganization is the Schema for the infisicalorganizations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of InfisicalOrganization.
            properties:
              connectionRef:
                description: |-
                  ConnectionRef selects the Infisical API connection used for organization lifecycle.
                  Creating or deleting an organization requires a user JWT or API key; machine identity
                  tokens can adopt an explicit organization after the platform has provisioned it.
                properties:
                  name:
                    description: Name is the InfisicalConnection name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              creationPolicy:
                default: Create
                description: CreationPolicy controls whether the operator creates
                  or adopts an organization.
                enum:
                - Create
                - Adopt
                - CreateOrAdopt
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether the external organization
                  is deleted with this resource.
                enum:
                - Delete
                - Orphan
                type: string
              organizationID:
                description: |-
                  OrganizationID identifies an existing organization to adopt. It is also the explicit
                  boundary used by tenant operators, which may not be able to call Infisical's user-only
                  organization lookup endpoint.
                minLength: 1
                type: string
              organizationName:
                description: |-
                  OrganizationName is the Infisical display name. If omitted, metadata.name is used.
                  It is required when creating an organization and is used for name-based adoption.
                maxLength: 64
                minLength: 1
                type: string
            required:
            - connectionRef
            type: object
            x-kubernetes-validations:
            - message: connectionRef is immutable; delete and recreate the InfisicalOrganization
              rule: '!has(oldSelf.connectionRef) || self.connectionRef == oldSelf.connectionRef'
            - message: organizationID is immutable; delete and recreate the InfisicalOrganization
              rule: '!has(oldSelf.organizationID) || self.organizationID == oldSelf.organizationID'
            - message: organizationName is immutable; delete and recreate the InfisicalOrganization
              rule: '!has(oldSelf.organizationName) || self.organizationName == oldSelf.organizationName'
            - message: organizationID cannot be set with creationPolicy Create; use
                Adopt or CreateOrAdopt
              rule: '!has(self.organizationID) || self.creationPolicy != ''Create'''
          status:
            description: Status defines the observed state of InfisicalOrganization.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the organization.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation reflected
                  in status.
                format: int64
                type: integer
              organizationID:
                description: OrganizationID is the immutable Infisical identifier
                  once created or adopted.
                type: string
              organizationName:
                description: OrganizationName is the observed Infisical display name.
                type: string
              slug:
                description: Slug is the observed Infisical organization slug.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
