---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: infisicalprojects.infisical.infisical-operator.io
spec:
  group: infisical.infisical-operator.io
  names:
    kind: InfisicalProject
    listKind: InfisicalProjectList
    plural: infisicalprojects
    singular: infisicalproject
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InfisicalProject is the Schema for the infisicalprojects API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of InfisicalProject
            properties:
              connectionRef:
                description: ConnectionRef selects the Infisical API connection.
                properties:
                  name:
                    description: Name is the InfisicalConnection name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              creationPolicy:
                default: Create
                description: CreationPolicy controls whether the operator creates
                  or adopts a project.
                enum:
                - Create
                - Adopt
                - CreateOrAdopt
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether the external project
                  is deleted with this resource.
                enum:
                - Delete
                - Orphan
                type: string
              description:
                description: Description is an optional project description.
                maxLength: 1024
                type: string
              hasDeleteProtection:
                default: false
                description: HasDeleteProtection configures Infisical-side delete
                  protection.
                type: boolean
              projectName:
                description: ProjectName is the Infisical project name. If omitted,
                  metadata.name is used.
                maxLength: 64
                minLength: 1
                type: string
              shouldCreateDefaultEnvs:
                default: true
                description: |-
                  ShouldCreateDefaultEnvs controls whether Infisical creates its default environments.
                  It is only used during project creation.
                type: boolean
              slug:
                description: Slug is an optional unique project slug.
                maxLength: 64
                minLength: 5
                type: string
              type:
                default: secret-manager
                description: Type selects the Infisical product type.
                enum:
                - secret-manager
                - cert-manager
                - kms
                - ssh
                - secret-scanning
                - pam
                - ai
                type: string
            required:
            - connectionRef
            type: object
            x-kubernetes-validations:
            - message: connectionRef is immutable; delete and recreate the InfisicalProject
              rule: '!has(oldSelf.connectionRef) || self.connectionRef == oldSelf.connectionRef'
            - message: shouldCreateDefaultEnvs is immutable; delete and recreate the
                InfisicalProject
              rule: '!has(oldSelf.shouldCreateDefaultEnvs) || self.shouldCreateDefaultEnvs
                == oldSelf.shouldCreateDefaultEnvs'
            - message: type is immutable; delete and recreate the InfisicalProject
              rule: '!has(oldSelf.type) || self.type == oldSelf.type'
          status:
            description: status defines the observed state of InfisicalProject
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the project.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              environments:
                description: Environments contains the environments returned by Infisical.
                items:
                  description: EnvironmentStatus describes an Infisical project environment.
                  properties:
                    id:
                      description: ID is the Infisical environment identifier.
                      type: string
                    name:
                      description: Name is the display name.
                      type: string
                    slug:
                      description: Slug is the stable environment slug.
                      type: string
                  required:
                  - id
                  - name
                  - slug
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation reflected
                  in status.
                format: int64
                type: integer
              organizationID:
                description: OrganizationID is the organization that owns the project.
                type: string
              projectID:
                description: ProjectID is the immutable Infisical identifier once
                  created or adopted.
                type: string
              slug:
                description: Slug is the observed Infisical project slug.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
