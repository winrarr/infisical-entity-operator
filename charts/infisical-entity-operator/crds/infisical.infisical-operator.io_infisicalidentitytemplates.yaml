---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: infisicalidentitytemplates.infisical.infisical-operator.io
spec:
  group: infisical.infisical-operator.io
  names:
    kind: InfisicalIdentityTemplate
    listKind: InfisicalIdentityTemplateList
    plural: infisicalidentitytemplates
    singular: infisicalidentitytemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InfisicalIdentityTemplate is the Schema for the infisicalidentitytemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InfisicalIdentityTemplateSpec defines an organization-owned
              identity authentication template.
            properties:
              authMethod:
                description: AuthMethod selects the template authentication method.
                enum:
                - ldap
                - kubernetes
                - oidc
                type: string
              connectionRef:
                description: ConnectionRef selects the Infisical API connection.
                properties:
                  name:
                    description: Name is the InfisicalConnection name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              creationPolicy:
                default: Create
                description: CreationPolicy controls whether the operator creates
                  or adopts a template.
                enum:
                - Create
                - Adopt
                - CreateOrAdopt
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether the external template
                  is deleted with this resource.
                enum:
                - Delete
                - Orphan
                type: string
              kubernetes:
                description: Kubernetes contains Kubernetes Auth template settings
                  when authMethod is kubernetes.
                properties:
                  allowedAudience:
                    description: AllowedAudience restricts the audience claim on authenticating
                      tokens.
                    maxLength: 1000
                    type: string
                  caCertSecretRef:
                    description: CACertSecretRef references a Secret containing the
                      PEM-encoded API CA certificate.
                    properties:
                      key:
                        description: Key is the Secret data key.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  gatewayID:
                    description: GatewayID selects an Infisical gateway.
                    format: uuid
                    type: string
                  gatewayPoolID:
                    description: GatewayPoolID selects an Infisical gateway pool.
                    format: uuid
                    type: string
                  kubernetesHost:
                    description: KubernetesHost is the Kubernetes API server URL used
                      for token review.
                    maxLength: 255
                    type: string
                  tokenReviewMode:
                    description: TokenReviewMode selects the API-server or gateway
                      token review path.
                    enum:
                    - api
                    - gateway
                    type: string
                  tokenReviewerJWTSecretRef:
                    description: TokenReviewerJWTSecretRef references a Secret containing
                      a TokenReview JWT.
                    properties:
                      key:
                        description: Key is the Secret data key.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  verifyTLSCertificate:
                    description: VerifyTLSCertificate controls API server certificate
                      verification.
                    type: boolean
                type: object
              ldap:
                description: LDAP contains LDAP template settings when authMethod
                  is ldap.
                properties:
                  bindDN:
                    description: BindDN is the LDAP bind distinguished name.
                    minLength: 1
                    type: string
                  bindPasswordSecretRef:
                    description: BindPasswordSecretRef references a Secret containing
                      the LDAP bind password.
                    properties:
                      key:
                        description: Key is the Secret data key.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  caCertSecretRef:
                    description: CACertSecretRef references a Secret containing the
                      LDAP CA certificate.
                    properties:
                      key:
                        description: Key is the Secret data key.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  searchBase:
                    description: SearchBase is the LDAP search base.
                    minLength: 1
                    type: string
                  url:
                    description: URL is the LDAP server URL.
                    minLength: 1
                    type: string
                required:
                - bindDN
                - bindPasswordSecretRef
                - searchBase
                - url
                type: object
              oidc:
                description: OIDC contains OIDC template settings when authMethod
                  is oidc.
                properties:
                  boundAudiences:
                    description: BoundAudiences is the comma-separated audience list.
                    maxLength: 2048
                    type: string
                  boundIssuer:
                    description: BoundIssuer is the expected JWT issuer.
                    maxLength: 2048
                    minLength: 1
                    type: string
                  caCertSecretRef:
                    description: CACertSecretRef references a Secret containing the
                      identity provider CA certificate.
                    properties:
                      key:
                        description: Key is the Secret data key.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  oidcDiscoveryURL:
                    description: OIDCDiscoveryURL is the identity provider discovery
                      URL.
                    format: uri
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - boundIssuer
                - oidcDiscoveryURL
                type: object
              organizationRef:
                description: OrganizationRef identifies the organization that owns
                  this template.
                properties:
                  name:
                    description: Name is the referenced resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              templateName:
                description: TemplateName is the Infisical template name. If omitted,
                  metadata.name is used.
                maxLength: 64
                minLength: 1
                type: string
            required:
            - authMethod
            - connectionRef
            - organizationRef
            type: object
            x-kubernetes-validations:
            - message: exactly one auth method configuration must match authMethod
              rule: (self.authMethod == 'ldap' && has(self.ldap) && !has(self.kubernetes)
                && !has(self.oidc)) || (self.authMethod == 'kubernetes' && has(self.kubernetes)
                && !has(self.ldap) && !has(self.oidc)) || (self.authMethod == 'oidc'
                && has(self.oidc) && !has(self.ldap) && !has(self.kubernetes))
            - message: connectionRef is immutable; delete and recreate the InfisicalIdentityTemplate
              rule: '!has(oldSelf.connectionRef) || self.connectionRef == oldSelf.connectionRef'
            - message: organizationRef is immutable; delete and recreate the InfisicalIdentityTemplate
              rule: '!has(oldSelf.organizationRef) || self.organizationRef == oldSelf.organizationRef'
            - message: authMethod is immutable; delete and recreate the InfisicalIdentityTemplate
              rule: '!has(oldSelf.authMethod) || self.authMethod == oldSelf.authMethod'
          status:
            description: InfisicalIdentityTemplateStatus defines the observed state
              of an identity template.
            properties:
              authMethod:
                description: IdentityTemplateAuthMethod selects the Infisical authentication
                  method configured by a template.
                enum:
                - ldap
                - kubernetes
                - oidc
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the template.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              kubernetes:
                description: IdentityTemplateKubernetesStatus exposes non-secret observed
                  Kubernetes Auth template fields.
                properties:
                  allowedAudience:
                    type: string
                  gatewayID:
                    type: string
                  gatewayPoolID:
                    type: string
                  hasCACertificate:
                    type: boolean
                  hasTokenReviewerJWT:
                    type: boolean
                  kubernetesHost:
                    type: string
                  tokenReviewMode:
                    description: KubernetesTokenReviewMode selects how Infisical validates
                      Kubernetes service account tokens.
                    enum:
                    - api
                    - gateway
                    type: string
                  verifyTLSCertificate:
                    type: boolean
                type: object
              ldap:
                description: IdentityTemplateLDAPStatus exposes non-secret observed
                  LDAP template fields.
                properties:
                  bindDN:
                    type: string
                  hasBindPassword:
                    type: boolean
                  hasCACertificate:
                    type: boolean
                  searchBase:
                    type: string
                  url:
                    type: string
                type: object
              name:
                type: string
              observedGeneration:
                format: int64
                type: integer
              oidc:
                description: IdentityTemplateOIDCStatus exposes non-secret observed
                  OIDC template fields.
                properties:
                  boundAudiences:
                    type: string
                  boundIssuer:
                    type: string
                  hasCACertificate:
                    type: boolean
                  oidcDiscoveryURL:
                    type: string
                type: object
              organizationID:
                type: string
              secretResourceVersions:
                description: |-
                  SecretResourceVersions identifies the referenced Secret versions represented in the remote template.
                  It contains no Secret data and is used to detect updates to write-only template fields.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              templateID:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
