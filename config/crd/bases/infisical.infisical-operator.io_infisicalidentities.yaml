---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: infisicalidentities.infisical.infisical-operator.io
spec:
  group: infisical.infisical-operator.io
  names:
    kind: InfisicalIdentity
    listKind: InfisicalIdentityList
    plural: infisicalidentities
    singular: infisicalidentity
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InfisicalIdentity is the Schema for the infisicalidentities API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of InfisicalIdentity
            properties:
              connectionRef:
                description: ConnectionRef selects the Infisical API connection.
                properties:
                  name:
                    description: Name is the InfisicalConnection name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              creationPolicy:
                default: Create
                description: CreationPolicy controls whether the operator creates
                  or adopts an identity.
                enum:
                - Create
                - Adopt
                - CreateOrAdopt
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether the external identity
                  is deleted with this resource.
                enum:
                - Delete
                - Orphan
                type: string
              hasDeleteProtection:
                default: false
                description: HasDeleteProtection configures Infisical-side delete
                  protection.
                type: boolean
              identityName:
                description: IdentityName is the Infisical identity name. If omitted,
                  metadata.name is used.
                minLength: 1
                type: string
              metadata:
                description: Metadata contains optional key/value metadata attached
                  to the identity.
                items:
                  description: IdentityMetadata is a key/value pair attached to an
                    Infisical identity.
                  properties:
                    key:
                      description: Key is the metadata key.
                      minLength: 1
                      type: string
                    value:
                      description: Value is the metadata value.
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - key
                x-kubernetes-list-type: map
              projectRef:
                description: ProjectRef references the InfisicalProject resource that
                  owns this identity.
                properties:
                  name:
                    description: Name is the referenced resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              roleSlugs:
                description: |-
                  RoleSlugs declares the permanent project role slugs assigned to the identity.
                  When omitted, the existing project membership is not managed. Use no-access
                  explicitly when the identity should have no project permissions.
                items:
                  type: string
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
            required:
            - connectionRef
            - projectRef
            type: object
            x-kubernetes-validations:
            - message: connectionRef is immutable; delete and recreate the InfisicalIdentity
              rule: '!has(oldSelf.connectionRef) || self.connectionRef == oldSelf.connectionRef'
            - message: projectRef is immutable; delete and recreate the InfisicalIdentity
              rule: '!has(oldSelf.projectRef) || self.projectRef == oldSelf.projectRef'
          status:
            description: status defines the observed state of InfisicalIdentity
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the identity.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              identityID:
                description: IdentityID is the immutable Infisical identifier once
                  created or adopted.
                type: string
              membershipID:
                description: MembershipID is the Infisical project membership identifier
                  when role management is enabled.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation reflected
                  in status.
                format: int64
                type: integer
              projectID:
                description: ProjectID is the observed owning project identifier.
                type: string
              roles:
                description: Roles contains the observed project roles when role management
                  is enabled.
                items:
                  description: IdentityRoleStatus describes an observed permanent
                    or temporary project role.
                  properties:
                    isTemporary:
                      description: IsTemporary reports whether Infisical marked the
                        assignment as temporary.
                      type: boolean
                    name:
                      description: Name is the custom role display name when available.
                      type: string
                    roleID:
                      description: RoleID is the Infisical role assignment identifier.
                      type: string
                    slug:
                      description: Slug is the built-in or custom role slug.
                      type: string
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
